#ifndef FILAMENT_NOISE_SLANGH
#define FILAMENT_NOISE_SLANGH

float FilamentInterleavedGradientNoise(float2 screenPos)
{
    float3 magic = float3(0.06711056, 0.00583715, 52.9829189);
    return frac(magic.z * frac(dot(screenPos.xy, magic.xy)));
}

#endif // FILAMENT_NOISE_SLANGH
